services:
  gateway:
    build: "./gateway"
    image: "gateway-ms:v1"
    container_name: "gateway"
    ports:
      - '8081:8081'
    expose:
      - '8081'

networks:
  cluster:
    driver: bridge
    

